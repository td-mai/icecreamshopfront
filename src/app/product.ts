export interface Product {
        id: number;
        flavor: string;
        unit_price: number;
        recipe: string;
        image: string;
        remaining_quantity: number;
        max_quantity: number;
        tmp_order_quantity: number;
}
